/*

Variáveis 

obs: pedaço de memoria no computador

let - pode ser alterado quando quiser
const - Só pode se declarado uma vez na variavel

obs: de regra sempre usar o const, porém quando houver necessidade usar o let.

*/

let numberStudents = [50,20]
const result = numberStudents[0] + numberStudents[1]
console.log(result);
